project:
  name: __PROJECT_NAME__

runtimes:
  claude:
    enabled: true
  codex:
    enabled: false
  opencode:
    enabled: false

sync:
  permissions: true

files:
  permissions: .ai/permissions.yaml
