project:
  name: __PROJECT_NAME__

agents:
  default: claude

runtimes:
  claude:
    enabled: true
  codex:
    enabled: false
  opencode:
    enabled: false

sync:
  permissions: true
  agents: false

files:
  permissions: .ai/permissions.yaml
  agents: .ai/agents.yaml
  rules: .ai/rules.md
  workflows: .ai/workflows
